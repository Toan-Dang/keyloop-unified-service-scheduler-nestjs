name: CI

on:
  push:
    branches: ['**']
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '24'
  # `prisma.config.ts` resolves DATABASE_URL via Prisma 7's `env()` helper, which throws if the
  # var is unset — so `prisma generate` needs *a* value even though it never connects. Real test
  # suites overwrite process.env.DATABASE_URL themselves once Testcontainers starts (see
  # test/support/test-database.ts); this placeholder only satisfies config loading.
  DATABASE_URL: 'postgresql://scheduler:scheduler_dev_pw@localhost:5432/scheduler?schema=public'

jobs:
  static:
    name: Lint · typecheck · build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: npx prisma generate
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run build

  test:
    name: Unit · integration · concurrency · e2e
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: npx prisma generate

      - name: Unit tests
        run: npm run test:unit

      # The GitHub runner has a real Docker daemon, so Testcontainers starts
      # postgres:18-alpine itself — the EXCLUDE constraint genuinely runs.
      - name: Integration tests (Testcontainers + real PostgreSQL)
        run: npm run test:integration

      - name: Concurrency proof (the signature test)
        run: npm run test:concurrency

      - name: End-to-end tests
        run: npm run test:e2e

  contract:
    name: OpenAPI contract is committed and current
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: npx prisma generate
      - run: npm run openapi:generate
      - name: Fail if openapi.yaml is stale
        run: git diff --exit-code -- openapi.yaml
